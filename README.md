# RateScout — мониторинг курсов обмена

Статический сайт-монитор курсов обмена криптовалют и денег на базе **BestChange**
(партнёрская программа). Хостинг — GitHub Pages, домен **ratescout.ru**.

## Статус (деплой)
- ✅ Репозиторий **github.com/sementsul/ratescout**, GitHub Pages (source=Actions), CI зелёный, автодеплой.
- ✅ Главная + **330 страниц валют** + комплаенс; sitemap 335 URL.
- ✅ **Реальные курсы** — из публичного дампа `api.bestchange.ru/info.zip` (без ключа), ~42k направлений, автообновление ежечасно по cron.
- ✅ **Автосинхрон каталога валют** — из того же дампа (`bm_cy.dat`): добавление/удаление валют и категории подхватываются сами каждый час; существующие слаги/URL сохраняются, новые валюты — числовые deep-links.
- ✅ **Heartbeat** (`keepalive.yml`) — еженедельный коммит-пульс против 60-дневного авто-отключения scheduled-workflow (cron не «засыпает»).
- ✅ **Дизайн под doshaven.eu** — тёмная DOS-тема: `#wrapper` 1000px, циан меню-бар, двойные ASCII-рамки (`.dosborder`), `#content`+`#sidebar`, красные ■ маркеры.
- ✅ **AML-кнопка** → реальный инструмент `bestchange.ru/report/?p=…`.
- ✅ **Правовой статус — информационный ресурс** (ОРД/ERID не применяем): по всему сайту убраны оценочные/призывные формулировки (лучший/выгодный/надёжный/…), тон нейтральный справочный; дисклеймер «не реклама/оферта/рекомендация».
- ✅ **Юр-страницы заполнены:** Политика 152-ФЗ (10 разделов), Раскрытие, «Что такое BestChange», AML. Оператор: самозанятый (НПД) Семенцул М.Г., ИНН 381616884622, контакт **sementsul.maksim@yandex.ru**.
- ✅ **Своя DOS-иконка** (⇄) favicon/PWA; логотип `[⇄]`.
- ✅ **AML-кнопка** → реальный инструмент `bestchange.ru/report/`.
- 🔺 Остаточный юр-риск (монетизация ссылками, AML-продукт) — финальная оценка за юристом.
- ⏭ **ratescout.ru** — нужен DNS (A-записи apex → Pages `185.199.108–111.153`), затем кастомный домен + SSL. На github.io/ratescout ассеты 404 (абсолютные пути — только для корня домена).
- 🔴 Токены/ключ, засвеченные при настройке, — перевыпустить.

## Что это
- Каталог **330 валют** (6 категорий) + страница на каждую валюту со всеми направлениями обмена.
- Конвертер направления по всему каталогу → deep-link в BestChange с реф-меткой.
- SEO: уникальные title/description/canonical, sitemap (335 URL), schema.org (FAQ/Breadcrumb), PWA.
- Комплаенс: страницы «что такое BestChange» / AML / раскрытие (FTC + ERID/ОРД + OFAC) / политика.

## Сборка
```bash
python3 build.py          # генерит dist/ (главная + 330 страниц валют + комплаенс)
```
Каталог валют — `currencies.json` (парсится из выгрузки BestChange через `parse_catalog.py`).

## Реальные курсы (Фаза 2)
`fetch_rates.py` тянет курсы по партнёрскому API и кладёт в `rates.json`, который `build.py` запекает
в страницы. Запускается в **GitHub Actions по расписанию** (`.github/workflows/deploy.yml`).

## 🔴 Безопасность ключа
Партнёрский ключ BestChange **никогда не попадает в браузер/репозиторий/сборку**:
- локально — в `.env` (в `.gitignore`);
- в CI — в **GitHub Secrets** (`BESTCHANGE_API_KEY`, `BESTCHANGE_RATES_URL`), используется только в раннере;
- workflow содержит guard: если ключ найден в `dist/` — деплой прерывается.

## Дисклеймер
RateScout — независимый информационный сервис, не обменный пункт. Ссылки ведут в BestChange; по партнёрской
программе возможно вознаграждение. Не является финансовой рекомендацией. 18+.
